<h1>Listing user_roles</h1>

<table>
  <tr>
    <th>User</th>
    <th>Role</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_roles.each do |users_role| %>
  <tr>
    <td><%= user_role.user_id %></td>
    <td><%= user_role.role_id %></td>
    <td><%= link_to 'Show', users_role %></td>
    <td><%= link_to 'Edit', edit_user_role_path(users_role) %></td>
    <td><%= link_to 'Destroy', users_role, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User Role', new_user_role_path %>
